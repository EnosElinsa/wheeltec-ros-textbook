name: Test ROS 2 examples

on:
  push:
    paths:
      - "docs/04-ros2-development/**"
      - ".github/workflows/test-ros2-examples.yml"
  pull_request:
    paths:
      - "docs/04-ros2-development/**"
      - ".github/workflows/test-ros2-examples.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    container: ros:humble-ros-base-jammy
    defaults:
      run:
        shell: bash
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Fetch textbook code repository
        run: git clone --depth 1 https://github.com/EnosElinsa/wheeltec-ros-source-reference.git /tmp/wheeltec-ros-source-reference

      - name: Create isolated ROS 2 workspace
        run: |
          mkdir -p /tmp/wheeltec_course_ws/src
          cp -r /tmp/wheeltec-ros-source-reference/examples/ros2-pubsub/cpp_pubsub /tmp/wheeltec_course_ws/src/
          cp -r /tmp/wheeltec-ros-source-reference/examples/ros2-pubsub/py_pubsub /tmp/wheeltec_course_ws/src/

      - name: Build packages
        run: |
          source /opt/ros/humble/setup.bash
          cd /tmp/wheeltec_course_ws
          colcon build --event-handlers console_direct+

      - name: Run package tests
        run: |
          source /opt/ros/humble/setup.bash
          source /tmp/wheeltec_course_ws/install/setup.bash
          cd /tmp/wheeltec_course_ws
          colcon test --event-handlers console_direct+
          colcon test-result --verbose
