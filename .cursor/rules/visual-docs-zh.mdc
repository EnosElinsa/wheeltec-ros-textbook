---
description: 中文技术文档写法。撰写或修改 R680 视觉建图与导航两篇独立说明时必须遵守。
globs:
  - docs/06-sensors-navigation/38-visual-runtime.md
  - docs/06-sensors-navigation/39-visual-operation.md
alwaysApply: false
---

# 中文技术文档

撰写或修改 `docs/06-sensors-navigation/38-visual-runtime.md` 与 `docs/06-sensors-navigation/39-visual-operation.md` 时遵守本规则。改代码、测试、启动文件、教材第 1～37 章与第 40～53 章时不要套用。

读者是读源码的人。写正式说明：像手册正文、规范、源码注释扩写，不像讲义、口播稿、宣传稿、随笔。

去掉套话不等于改成口语。不要为了「自然」加入比喻、口号、第一人称、幽默或课堂口吻。自然来自把事情说清楚，不是来自把句子写短、写俏皮。

写完后按文末检查表通读一遍。凡命中禁止项，先改再交。

## 结构

全文先总后分。开篇说明整条过程、建图与导航的差别、数据按什么顺序传递。过程概要对相机、投射器、视觉里程计、TF、地图、定位、规划、执行等部件各用一两段说明它是什么、在过程中做什么。不写阈值、字段布局、启动参数；那些放在对应章节。

每一章开头说明这一步做什么、与上下游如何衔接、产生哪些话题或文件。然后按小节写细节。不要在章首预告「下面先讲 A 再讲 B」。小节标题就是顺序，读者自己往下看。

先把事情讲清楚，再给出对应的话题名、消息字段、启动参数和判定数字。不要用参数表当开场。参数、字段、YAML 示例放在该事物已经交代之后。

本包文档独立成篇。不要引用、对照或提及操作手册，不要写「与手册的关系」。概念在本文写一次即可。

## 语气

直接陈述。优先用：是、有、订阅、发布、变换到、写入、读取、启动。

句子说事实。不要先否定再肯定，不要为防误解而加旁白。读者能从上下文看懂的，不重复声明。

## 标题

标题是名词或名词短语，像章节名。不要口语句、问句、对仗、口号、板书。

不要在标题里做选择（「用 A，不用 B」）、解释动机（「为什么要…」）、盘点内容（「…里有什么」）、加条件口语（「…什么时候会…」）。

```markdown
# 不好
## 用左红外，不用彩色
## 图像里有什么
## 投射器为什么要交错开关
## 底盘什么时候会启动
## 跟丢时把速度截住
## 整条链路分成两段

# 好
## 左红外与深度
## 图像与内参
## 投射器交错
## 底盘启动
## 视觉跟丢
## 过程概要
```

章名用过程中的步骤名：感知、运动估计、TF、建图、定位、规划、执行。不要用「第 N 站」「先看相机」这类口播名。

## 禁止：口头语和比喻

不要用动作比喻代替 ROS 操作。

禁止：吃话题、拉起节点、一拍、走读、走通、写死、钉死、加料、截住、夹在中间、挂在、对上、变到车上、端出来、兜住、托住、垫一层、拧过来。

对应写法：订阅、启动、发布、固定为、写入、接在 A 与 B 之间、变换到、发布到。

## 禁止：装腔和自造压缩语

不要把简单过程说成架构黑话，不要给普通对象起戏剧性称呼。

禁止：链路、两段用法、同一套前半段、入口、出口、一份 RGB-D、装在同一份消息里、能当特征的红外、深度较好的那一帧、观测端、执行端、前半段、后半段（作结构隐喻时）。

「入口 / 出口」不要用来指话题。表格也不要写「入 / 出」。写订阅、发布，或直接写话题名。

「一份」只用于计数（各发布一份 `CameraInfo`、转发最近一份 `/cmd_vel`）。不要写「一份 RGB-D」来称呼同步后的消息。

ROS 术语用通用译法或英文：订阅、发布、坐标系、变换、消息、话题、节点、启动文件。不要自造压缩语。接口历史上叫 RGB-D 的，正文可沿用该接口名，并说明内容是红外加深度。

## 禁止：口播和讲义腔

禁止：下面先说、再看、然后是、接下来我们、这里先、简单说、换句话说、也就是说、可以看到、需要注意的是、值得一提。

章首不要目录预告。小节标题承担导航。

## 禁止：防御句和冗余否定

不要写「A 并不表示 B」「进程在不等于有数据」「X 不是 Y 而是 Z」「不怎么…」「并不是说…」。

例外：该句纠正一个会写进代码或启动参数的具体误解，且改成肯定句会丢信息。例如：像素值 0 表示无效深度，不是距离为零；`hold_odom` 判断用 `start_base`，不是 `start_chassis`。即便如此，能拆成两句肯定陈述时优先拆开。

独立文档写一次概念即可，不要为了显得严谨而反复声明「不是某某」。

实机尚未核实的量，写「需在实机读取」，附录给命令。不要免责段，不要「本文不保证」。

## 禁止：书面套话和戏剧性标点

禁止：须、务必、应当、至关重要、不仅…而且…、希望这对您有帮助、请注意、切勿忘记。

不要用「」给普通技术事实加戏剧性称呼。引号用于消息字段名、参数名、或原文引用。

## 用语对照

```markdown
# 不好
整条链路分成两段用法、同一套前半段。
感知把相机变成后面能用的观测。
装在同一份消息里。
入口是 D435C，出口是一份 RGB-D。
后面才能把像素变到车上。
「能当特征的红外」和「深度较好的那一帧」。
节点已启动，并不表示话题上已有有效数据。
视觉里程计不能直接吃两路原始话题。
下面先说为什么用左红外而不是彩色。

# 好
建图和导航共用从相机到视觉里程计这一段。
相机发布红外图像和深度图像。视觉里程计订阅同步后的红外与深度。
写入一条 `rtabmap_msgs/msg/RGBDImage`。
相机为 D435C。同步后的消息发布在 `/rtabmap/rgbd_image`。
才能把相机坐标系中的点变换到车体坐标系。
关掉投射器的红外图像没有散斑；打开投射器时深度更稳定。
（删掉。需要时写：部分话题只在对应启动运行之后才出现。）
视觉里程计订阅同步后的 RGB-D，不订阅两路原始图像话题。
（删掉预告，直接写左红外与深度的事实。）
```

## 写法检查表

交稿前逐项确认：

1. 开篇是过程总述，并对相机、里程计、TF、地图、规划、执行等部件作概念说明；没有阈值、字段或启动参数堆叠。
2. 每章先交代该步做什么，再进入小节；章首没有「下面先…」。
3. 全部标题都是名词或名词短语。
4. 没有入口/出口、链路、一份 RGB-D、变到车上、装在同一份消息里。
5. 没有吃、拉、一拍、走读、截住、夹在、挂在等比喻。
6. 没有「并不表示」「不等于」「不怎么」类旁白。
7. 没有口号标题、问句标题、对仗标题。
8. 话题与消息用订阅/发布/变换到，并写出名字。
9. 未核实的量只标「需在实机读取」，并在附录给命令。
10. 未提及操作手册。
